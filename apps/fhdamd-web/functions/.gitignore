# Compiled JavaScript files
lib/**/*.js
lib/**/*.js.map

# TypeScript v1 declaration files
typings/

# Node.js dependency directory
node_modules/
*.local

# Non-secret runtime params (defineString) — deliberately committed, unlike
# the app's own .env which the parent .gitignore excludes for real secrets.
!.env
