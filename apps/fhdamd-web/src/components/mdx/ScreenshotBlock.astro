---
import { ScreenshotFigure } from "@fhdamd/threads";

interface Props {
  url?: string;
  alt?: string;
  caption?: string;
}

const { url, alt, caption } = Astro.props;
---

<ScreenshotFigure src={url} alt={alt} caption={caption} />
