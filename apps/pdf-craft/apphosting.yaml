# Firebase App Hosting configuration has moved to the repository root
# (/apphosting.yaml) so that Firebase clones the full monorepo and the
# Astro build can resolve packages/threads/src via the Vite alias.
# This file is intentionally left here for reference only.
