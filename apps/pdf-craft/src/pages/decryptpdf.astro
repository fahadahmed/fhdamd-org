---
import Layout from "../layouts/Layout.astro";
import { DecryptPdf } from "../components";

if (!Astro.cookies.has('__session')) {
  return Astro.redirect('/signin')
}
---

<Layout title="Unlock PDF">
  <DecryptPdf client:load />
</Layout>
